---
import "../styles/style.css";
import SecondaryButton from "./SecondaryButton.astro";

const { title, description, imgPath, link_side, backgroundImage } = Astro.props;
---
    <!-- <div class="flex justify-center items-center h-screen card">
        <div class=" bg-[url('/kategoricards/kaffeprodukter.webp')] bg-cover w-96 h-96 overflow-hidden shadow-lg hover:shadow-2xl duration-150">
                    <a href={link_side}> 


            <div class="p-6 text-white  pt-46">
                <h2 class=" font-bold mb-2">{title}</h2>
                           <div class="flex justify-between">
                <p class="w-[15rem]" >{description}</p>
                <SecondaryButton></SecondaryButton>
                </div>
            </div>
          </a>
        </div> -->
       

    </div>
    <div class="flex justify-center">
<div style={`background-image: url(${backgroundImage})`} class="bg-cover w-full h-full overflow-hidden shadow-lg hover:shadow-2xl duration-100 " >
                    <a href={link_side}> 
            <!-- Content inside the card goes here -->
            <div class="p-6 text-white pt-52">
                <h2 class="font-bold mb-2">{title}</h2>
                           <div class="flex justify-between">
                <p class="w-[15rem]" >{description}</p>
                <SecondaryButton></SecondaryButton>
                </div>
            </div>
          </a>
        </div>
       
    </div>

